{
  "name": "auth-decoupled",
  "module": "index.ts",
  "scripts": {
    "dev": "concurrently \"bun run dev --cwd frontend-admin\" \"bun run dev --cwd frontend-user\" \"bun run dev --cwd hono-backend\""
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}